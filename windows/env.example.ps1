# Machine-local environment variables, dot-sourced at the end of windows\profile.ps1.
# install.ps1 copies this file to windows\env.ps1 when that file is missing; env.ps1 is
# gitignored so keys and tokens stay out of the repo.

# $env:SOME_API_KEY = "..."
